export function Button(label: string) {
  return <button>{label}</button>;
}

//example stuff, delete later 
//
const Btn = (prop: string) => { 
    return <button>{prop}</button>
}

type BtnType = {
    prop: string,
  }
